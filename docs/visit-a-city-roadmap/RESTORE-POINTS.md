# აღდგენის წერტილები

ეს ფაილი აღწერს ლოკალურ Git snapshot-ებს, რომლებითაც პროექტის სრული სამუშაო მდგომარეობის დაბრუნებაა შესაძლებელი.

## 2026-08-20 — PERF-01-ის შემდეგ

- Git tag: `restore-2026-08-20-perf01`
- დანიშნულება: PERF-01-ის დასრულებისა და ხარისხის შემოწმებების შემდეგ არსებული სრული მდგომარეობა.
- snapshot მოიცავს ამ მომენტში Git-ის მიერ კონტროლირებად ყველა შეცვლილ და ახალ სამუშაო ფაილს.

### უსაფრთხოდ დათვალიერება

არსებული სამუშაოს დაკარგვის გარეშე ამ მდგომარეობიდან ცალკე branch-ის შექმნა:

```powershell
git switch -c restore-check-2026-08-20 restore-2026-08-20-perf01
```

### პროექტის ზუსტად დაბრუნება

ზუსტი დაბრუნება ცვლის მიმდინარე branch-ის მდგომარეობას და ამიტომ უნდა შესრულდეს მხოლოდ ახალი ცვლილებების ცალკე snapshot-ში შენახვის შემდეგ:

```powershell
git reset --hard restore-2026-08-20-perf01
```

არ გამოიყენოთ `git reset --hard`, თუ მიმდინარე დაუმახსოვრებელი ცვლილებების დაკარგვა არ გსურთ.
