services:
  - type: web
    runtime: static
    name: fleet-house
    plan: free
    buildCommand: pip install -r requirements.txt && python3 build.py dist
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: false
